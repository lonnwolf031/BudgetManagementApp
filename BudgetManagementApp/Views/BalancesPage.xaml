<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:data="clr-namespace:BudgetManagementApp.Data"
             x:Class="BudgetManagementApp.Views.BalancesPage"
             xmlns:local="clr-namespace:BudgetManagementApp;assembly=BudgetManagementApp"
             xmlns:viewmodels="clr-namespace:BudgetManagementApp.ViewModels" 
             Title="Balances">
  <ContentPage.Content>
    <ScrollView>

    <ListView x:Name="listView" SeparatorVisibility="None" ItemsSource="{x:Static viewmodels:BalancesViewModel.balances}" ItemSelected="listSelection">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Orientation="Horizontal" Padding="1">
              <Label Text="{Binding Name}" HorizontalOptions="CenterAndExpand" FontSize="18" FontFamily="" />
              <Label Text="{Binding LatestUpdate, StringFormat='{0:MMMM dd, yyyy}'}"  HorizontalOptions="CenterAndExpand" FontSize="18" FontFamily="" />
              <Label Text="{Binding ExpectedBalance, StringFormat='{0:D2}'}"  HorizontalOptions="CenterAndExpand" FontSize="18" FontFamily="" />
              <Label Text="{Binding RealBalance, StringFormat='{0:D2}'}" HorizontalOptions="End"  />
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>


  </ScrollView>
  </ContentPage.Content>
  <ContentPage.ToolbarItems>
    </ContentPage.ToolbarItems>

</ContentPage>